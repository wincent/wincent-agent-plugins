---
description: >-
  Access Jira data via the Atlassian CLI (`acli`). Use when the user asks about Jira issues, projects, workitems, sprints, or otherwise needs to interact with Jira.
allowed-tools:
  - Bash
---

# Accessing Jira data

Use the Atlassian CLI tool (`acli`) to interact with Jira.

- `acli jira --help` shows the available Jira subcommands.
- For help on a specific subcommand, append `--help`; for example:
  - `acli jira project --help`
